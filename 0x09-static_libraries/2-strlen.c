#include "holberton.h"
/**
 * _strlen - calculate the length of a string
 * @s: string
 * Return: value of length
 */

int _strlen(char *s)
{

int i;

for (i = 0; s[i] != '\0'; i++)
{

}
return (i);
}
